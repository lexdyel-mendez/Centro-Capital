{
  "name": "Centro-Capital",
  "version": "1.0.0",
  "scripts": {
    "install-frontend": "npm install --prefix frontend/centro-capital-frontend",
    "build-frontend": "npm run build --prefix frontend/centro-capital-frontend",
    "postinstall": "npm run install-frontend && npm run build-frontend"
  }
}